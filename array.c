#include<stdio.h>
void main()
{
int k,i,n,a[i],sum=0;
printf("enter the numbers");
scanf("%d",&n);
for(i=0;i<=n;i++)
{
scanf("%d",&a[i]);
}
printf("enter the k values");
scanf("%d",&k);
if(i<=k)
{
sum=sum+a[i];
}
printf("sum is %d",sum);
}
